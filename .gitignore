node_modules/
.DS_Store

# Secrets — never commit
.env
.env.*
*.env

# Build output — generated by `pnpm build` (tsc + index build), not committed.
# Consume the prebuilt index via GitHub Releases, or build locally.
dist/