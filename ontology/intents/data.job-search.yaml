id: data.job_search
label: Search job postings and hiring feeds
description: Query fresh job listings from ATS platforms and job boards with filters. Use when an agent tracks open roles, monitors hiring trends, or surfaces opportunities by title, company, or location.
aliases:
  - job search API
  - find open positions
  - hiring feed
  - ATS job listings
  - search job postings
  - career page jobs
  - job board search
  - filter jobs by role
  - fresh ATS job feed
  - linkedin jobs scraper
  - scrape job postings
  - job listings scraper
  - scrape jobs from job board
consumes:
  - { entity: Query, role: identifier }
  - { entity: Company, role: identifier }
  - { entity: Place, role: identifier }
produces:
  - { entity: Answer, role: payload, cardinality: many }
facets:
  domain: data
  action: search