id: ai.embeddings
label: Generate text embeddings for retrieval
description: Dense vector embeddings for semantic search, clustering, and RAG pipelines. Use when an agent needs to embed sentences or documents for retrieval, ranking, or vector indexing without hosting an embedding model.
aliases:
  - dense vectors
  - sentence embeddings
  - vector indexing
  - semantic embedding API
  - batch embeddings
  - turn sentences into vectors
  - text embeddings for retrieval
  - embedding API for semantic search
  - vectorize text batch
consumes:
  - { entity: Text, role: identifier }
produces:
  - { entity: Embedding, role: payload }

facets:
  domain: ai
  action: transform
