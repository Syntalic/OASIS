id: devtools.webhook_tools
label: Build, test, and sign webhook payloads
description: Webhook utilities — construct payloads, sign with HMAC, send test events, validate delivery, and dispatch HTTP callbacks. Use when an agent integrates or debugs webhook flows.
aliases:
  - webhook payload builder
  - sign webhook with HMAC
  - send test webhook event
  - webhook delivery test API
  - webhook signature validation
  - dispatch HTTP webhook callback
  - verify webhook signature
  - test a webhook endpoint
consumes:
  - entity: Query
    role: identifier
produces:
  - entity: Text
    role: payload
facets:
  domain: devtools
  action: execute